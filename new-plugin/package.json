{
  "name": "obsidian-callout-modal",
  "version": "0.1.0",
  "description": "Insert Obsidian callouts via a modal dialog with hotkey support.",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --outfile=main.js --external:obsidian --format=cjs --sourcemap --watch",
    "build": "esbuild src/main.ts --bundle --outfile=main.js --external:obsidian --format=cjs --minify"
  },
  "keywords": ["obsidian", "callout"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.0.0"
  }
}
